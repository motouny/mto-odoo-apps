<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.chroma.identity.theme</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Chroma Identity" string="Chroma Identity" name="chroma_identity_theme">
                    <block title="Live Preview" help="See your brand identity in action - navbar, headings, body text and buttons - before you save anything">
                        <widget name="chroma_identity_theme_preview"/>
                    </block>
                    <block title="Brand Colors" help="Full control of your platform's color identity - pick a preset or set your own">
                        <setting help="Detects your primary/accent colors straight from the company logo (Settings &gt; General Settings &gt; Companies), the same way Odoo's own Website Configurator does">
                            <button name="action_chroma_extract_logo_colors" type="object"
                                    string="Extract colors from logo" class="btn btn-secondary"/>
                        </setting>
                        <setting help="Choose a starting palette, or Custom to set your own colors below">
                            <field name="chroma_color_preset" widget="chroma_preset_radio"/>
                        </setting>
                        <setting help="Your main brand color - buttons, links and highlighted elements. Applied instantly, no page rebuild needed">
                            <field name="chroma_primary_color" widget="chroma_color_picker"/>
                        </setting>
                        <setting help="A secondary brand color used for hover and active states">
                            <field name="chroma_accent_color" widget="chroma_color_picker"/>
                        </setting>
                        <setting help="The dark background of the backend navbar, the app sidebar and their card-launcher dropdowns - separate from the primary/accent brand colors above">
                            <field name="chroma_chrome_color" widget="chroma_color_picker"/>
                        </setting>
                    </block>
                    <block title="Layout" help="Shape how the backend navigation looks and feels">
                        <setting help="Turn off to use the standard top-navbar app dropdown only, without the left sidebar. Takes effect after reloading the page.">
                            <field name="chroma_sidebar_enabled" string="Show Backend App Sidebar"/>
                        </setting>
                    </block>
                    <block title="Typography" help="Arabic + Latin font pairing used across backend, portal and website">
                        <setting help="Curated Google Fonts pairs, each covering both Arabic and Latin scripts cleanly">
                            <field name="chroma_font_pair"/>
                        </setting>
                    </block>
                    <block title="Text Colors" help="Fine-tune heading, body and secondary text colors for readability">
                        <setting help="Used for h1-h6 and bold titles">
                            <field name="chroma_heading_color" widget="chroma_color_picker"/>
                        </setting>
                        <setting help="Used for regular paragraph text">
                            <field name="chroma_body_color" widget="chroma_color_picker"/>
                        </setting>
                        <setting help="Used for captions, hints and secondary text">
                            <field name="chroma_muted_color" widget="chroma_color_picker"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_chroma_identity_theme_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'chroma_identity_theme'}</field>
    </record>
</odoo>
