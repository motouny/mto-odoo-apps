<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="mail_template_booking_confirmation" model="mail.template">
        <field name="name">Booking: Confirmation</field>
        <field name="model_id" ref="model_booking_order"/>
        <field name="subject">Your booking {{ object.name }} is confirmed</field>
        <field name="email_from">{{ (object.company_id.email or user.email) }}</field>
        <field name="email_to">{{ object.partner_id.email }}</field>
        <field name="auto_delete" eval="True"/>
        <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p>Dear <t t-out="object.partner_id.name or ''"/>,</p>
    <p>
        Your booking <strong t-out="object.name or ''"/> for a total of
        <strong t-out="format_amount(object.amount_total, object.currency_id)"/>
        has been confirmed.
    </p>
    <p>Thank you for booking with <t t-out="object.company_id.name or ''"/>.</p>
</div>
        </field>
    </record>
</odoo>
