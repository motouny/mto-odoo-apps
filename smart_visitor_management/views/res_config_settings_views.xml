<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.smart.visitor.management</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Visitor Management" string="Visitor Management"
                     name="smart_visitor_management"
                     groups="smart_visitor_management.group_visitor_manager">
                    <block title="Visitor Management">
                        <setting help="Require manager approval before a QR check-in code is issued">
                            <field name="visitor_require_approval"/>
                        </setting>
                        <setting help="Minutes before the scheduled start time during which check-in is allowed">
                            <field name="visitor_default_entry_grace_minutes"/>
                        </setting>
                        <setting help="Automatically expire approved visits whose end time has passed">
                            <field name="visitor_auto_expire_enabled"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_visitor_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'smart_visitor_management'}</field>
    </record>
</odoo>
